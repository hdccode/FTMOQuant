[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ftmoquant"
version = "0.1.0"
description = "Quantitative trading research tooling for FTMO."
readme = "README.md"
requires-python = ">=3.12,<3.13"
dependencies = [
    "arch==8.0.0",
    "nautilus-trader==2.0.0rc2",
    "pandas>=2.3.3,<4.0.0",
    "pyarrow>=16.1,<23.0.0",
    "pyyaml>=6.0.3",
    "tradedesk-dukascopy==1.0.0",
]

[project.scripts]
ftmoquant-ingest-eurusd = "ftmoquant.data.dukascopy:main"
ftmoquant-derive-eurusd-bars = "ftmoquant.data.derived_bars:main"
ftmoquant-qa-eurusd-coverage = "ftmoquant.data.session_coverage:main"

[dependency-groups]
dev = [
    "mypy>=1.15",
    "pytest>=8.3",
    "ruff>=0.11",
    "types-pyyaml>=6.0.12.20260724",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff]
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[tool.mypy]
python_version = "3.12"
strict = true
packages = ["ftmoquant"]
