universe_id: g1_4_fx_majors_v1
version: 1

source:
  market_data_origin: Dukascopy
  distribution_source: Hugging Face + direct Dukascopy cutoff completion
  huggingface_repo: mito0o852/dukascopy-ticks
  huggingface_revision: bf19dbd89c732f010e20db7c148922ba02b2e33b
  data_use_rights_status: unconfirmed_release_blocker

permitted_range:
  start_utc: "2019-03-11T00:00:00Z"
  end_exclusive_utc: "2024-08-21T00:00:00Z"
  holdout_cutoff_utc: "2024-08-21T00:00:00Z"

splits:
  development:
    start: "2019-03-11"
    end: "2023-04-10"
    days: 1492
  validation:
    start: "2023-04-11"
    end: "2024-08-20"
    days: 498

# Prospective majors universe. Only EUR/USD and GBP/USD are listed today:
# a mechanical source-inventory preflight against the exact pinned Hugging
# Face revision above found that NONE of the other three intended majors
# currently have complete DEVELOPMENT [2019-03-11, 2023-04-11) shard
# coverage, so none may be declared here yet (see the source-inventory
# preflight report for exact gap dates/day counts and per-pair status):
#
#   AUD/USD.DUKASCOPY: mid-range gaps 2019-07-14..2020-01-09 and
#     2020-02-09..2023-04-10 (1337/1492 development days missing).
#     discover_instrument_source_plan() rejects. No existing reconciliation
#     or direct-cutoff-completion mechanism can repair a mid-sequence gap
#     (both only operate on the trailing edge past the last usable shard).
#     REQUIRES_NEW_SOURCE_PATH.
#   USD/CHF.DUKASCOPY: mid-range gap 2020-10-06..2022-03-29 plus a further
#     trailing gap 2022-04-29..2023-04-10 (887/1492 development days
#     missing). discover_instrument_source_plan() rejects. Same
#     REQUIRES_NEW_SOURCE_PATH conclusion as AUD/USD.
#   USD/JPY.DUKASCOPY: single trailing gap 2022-11-25..2023-04-10
#     (137/1492 development days missing; no mid-range gaps at all).
#     discover_instrument_source_plan() rejects a DEVELOPMENT-scoped
#     request, BUT this is exactly the shape the existing
#     ingest_hf_instrument(..., direct_cache_root=...) direct-cutoff-
#     completion mechanism was built for -- it only activates when the
#     ingest is requested through the true holdout_cutoff_utc (not a
#     DEVELOPMENT-only request). READY_VIA_EXISTING_RECONCILIATION, not
#     yet executed. Add USD/JPY.DUKASCOPY here once that ingest has run
#     and the resulting canonical data has been verified to cover
#     DEVELOPMENT in full.
#
# USD/CAD and NZD/USD remain excluded: zero shards for either symbol exist
# in this dataset revision (confirmed identical to the latest revision).
instruments:
  - dataset_symbol: EURUSD
    instrument_id: EUR/USD.DUKASCOPY
    base_currency: EUR
    quote_currency: USD
    price_precision: 5
    price_increment: "0.00001"
    size_precision: 8
    size_increment: "0.00000001"
    session_policy_id: dukascopy-fx-ny-close-v1
  - dataset_symbol: GBPUSD
    instrument_id: GBP/USD.DUKASCOPY
    base_currency: GBP
    quote_currency: USD
    price_precision: 5
    price_increment: "0.00001"
    size_precision: 8
    size_increment: "0.00000001"
    session_policy_id: dukascopy-fx-ny-close-v1
